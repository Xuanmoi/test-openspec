## 1. 数据模型与 Store

- [x] 1.1 在 `WorkoutSet` 类型中增加 `reps: number` 字段
- [x] 1.2 更新 `createEmptySet(setNumber?)`：支持传入组别序号，默认 `reps` 为待填状态
- [x] 1.3 更新 `createEmptyGroup()`：默认生成 4 个小组（组别 1–4）

## 2. 表单布局与交互

- [x] 2.1 重构 `WorkoutForm` 小组行：组别、重量、个数 `grid-cols-3` 一行展示
- [x] 2.2 备注移至个数后方，增加「添加备注」勾选框，勾选后才显示输入框
- [x] 2.3 编辑已有备注的小组时，默认勾选并展开备注输入框
- [x] 2.4 移除「暂无小组」空状态对大组默认四小组的依赖（新大组始终有 4 组）

## 3. 校验与提交

- [x] 3.1 增加 `reps` 校验：正整数 ≥ 1
- [x] 3.2 提交时清理未勾选备注的空 `notes` 字段
- [x] 3.3 确认 `addSet` 追加逻辑：新小组组别为 max + 1

## 4. 详情展示

- [x] 4.1 更新 `workouts/[id].vue`：每个小组展示个数，无值显示「—」

## 5. 验证

- [x] 5.1 手动验证：新建大组默认 4 小组、删除/追加、备注勾选、保存与编辑
- [x] 5.2 运行 `npm run build`，确认无类型与构建错误
- [x] 5.3 检查 linter，确认无新增错误
